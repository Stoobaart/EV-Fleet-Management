import { StatCard } from '../../../shared/components/StatCard/StatCard'

export function VehicleAnalyticsCard({ totalVehicles }) {
  return <StatCard label="Total Vehicles" value={totalVehicles} />
}
